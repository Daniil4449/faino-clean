# DEVELOPMENT LOG — Faino Clean

## 2026-08-08 — Ініціалізація проєкту, Фаза 1 (сайт + форма)

**Контекст:** Новий, повністю окремий репозиторій (не форк і не калька проєкту Importica). Дизайн-система і структура сторінки — з ТЗ користувача. Перед стартом переглянуто репозиторій `Daniil4449/importica` як референс *інфраструктурних патернів* (SEO, деплой, форми) — дизайн і код не копіювались.

**Погоджений план (послідовно, фазами):**
1. ✅ Фаза 1 — сайт на чистому HTML/CSS/JS + Formsubmit для форми (без бекенду) + базове SEO + GitHub Pages
2. ⏳ Фаза 2 — Google Sheets webhook (Apps Script) для збору заявок у таблицю
3. ⏳ Фаза 3 — Telegram-бот через існуючий акаунт Make.com (окремий номер/бот від Importica)
4. ⏳ Фаза 4 — підключення домену fainoclean.com.ua (Cloudflare DNS, той самий підхід що і importica.com.ua)

**Що зроблено у Фазі 1:**
- `index.html` — повна структура: header (sticky) → hero → стрічка типів об'єктів (6) → послуги (плоский список, 6) → "чому ми" (темна секція, 5 пунктів) → історія (2013 → 2014 Київ) → контакти + форма → footer
- Дизайн-система: OKLCH-палітра, Playfair Display (заголовки) + Outfit (текст) з Google Fonts, тонкі лінійні SVG-іконки (без кольорових підложок), без нумерації розділів 01/02/03
- Анімації: scroll-reveal (fade + translateY), transitions 100–160ms ease-out, `prefers-reduced-motion` враховано
- Доступність: `:focus-visible`, мобільна адаптивність (breakpoints 880px / 560px)
- SEO: title/description/keywords, canonical, Open Graph + Twitter Card, JSON-LD `LocalBusiness`, `robots.txt`, `sitemap.xml`, `og-image.svg` (1200×630, брендоване)
- Форма заявки: Formsubmit.co, POST у прихований iframe (щоб не покидати сторінку), inline статус успіху
- Деплой: `.github/workflows/deploy.yml` (GitHub Actions → GitHub Pages), `CNAME` → fainoclean.com.ua

**Що потребує ручних дій користувача (не може зробити Claude Code):**
- [ ] **Formsubmit активація**: перший лист на `cleanandclear4449@gmail.com` від Formsubmit міститиме посилання підтвердження — його треба один раз відкрити, інакше форма не працюватиме
- [ ] **GA4**: створити властивість в Google Analytics → отримати Measurement ID (`G-XXXXXXXXXX`) → вставити в `<head>` замість коментаря-заглушки
- [ ] **GitHub repo**: створити репозиторій, увімкнути GitHub Pages у Settings → Pages → Source: GitHub Actions
- [ ] **Домен**: перевести DNS fainoclean.com.ua з imena.ua на Cloudflare (як зроблено для importica.com.ua), додати A/CNAME записи на GitHub Pages
- [ ] **Google Search Console**: підключити після деплою на реальний домен, відправити sitemap.xml

**Наступний крок:** ✅ репозиторій `Daniil4449/faino-clean` створено й запушено, GitHub Pages увімкнено, живий на https://daniil4449.github.io/faino-clean/.

## 2026-08-08 — Виправлення лого

Перша версія іконки (в шапці, hero, favicon, og-image) була намальована вручну і випадково вийшла як будиночок з дахом і дверима — саме той варіант, який брифом заборонено використовувати зараз. Виправлено:
- Отримано від користувача фінальні файли: `logo-green.svg`, `logo-black.svg` (повний лого-блок: напис "Faino Clean" + іконка будівлі, оригінальний вектор, вставлено без жодних змін), і окремо `logo-building-green.svg` (тільки іконка будівлі, без напису).
- Шапка і hero-блок тепер використовують `logo-green.svg` як `<img>`.
- `favicon.svg` зібрано з `logo-building-green.svg` (той самий вектор, без змін) + фонова підложка кольору paper для контрасту в табі браузера; підключено і як `apple-touch-icon` (застереження: Apple офіційно очікує PNG для touch-icon, SVG може не відображатись на Home Screen iOS — можна донести PNG пізніше, якщо знадобиться).
- Футер лишається текстовим (без іконки) — за прямою вимогою користувача.
- `og-image.svg` поки на попередній вручну намальованій (вже виправленій на будівлю, не будиночок) іконці — не оновлено оригінальним вектором, можна поправити пізніше за потреби.

## 2026-08-08 — Колір бренду, адаптивність, конкурентний аналіз, контакти

**Колір:** вихідний зелений з файлів лого (`#133E25`) на деяких моніторах читався як майже чорний. За запитом користувача пройшли два кроки корекції: спершу освітлили до `#1B6E3C`, потім за фідбеком затемнили на два тони до фінального `#1A5E33`. Змінено в `logo-green.svg`, `logo-building-green.svg`, `favicon.svg` і CSS-змінній `--accent` (щоб кнопки/іконки збігались з лого). Перевірено пікселем через headless-браузер, що колір реально застосувався. **Важливо:** `--ink` (колір заголовків/тексту) — окрема змінна, навмисно майже чорна (`oklch(24% 0.045 155)`) за задумом ще з початкового ТЗ ("основний темно-зелений текст") — це НЕ помилка і НЕ те саме, що `--accent`; користувачу пояснено різницю, залишили як є.

**Адаптивність:** підтверджено (виміряно `scrollWidth` vs `innerWidth` через JS, не на око) відсутність горизонтального overflow на 320px, 375px, 768px, 1024×600 (нетбук) і 1920px. Додано breakpoint `@media (max-width:380px)` для дуже вузьких екранів.

**Конкурентний аналіз:** досліджено ринок клінінгу для бізнесу в Україні, детально розібрано [Forline](https://forline.ua/en/) (найближчий B2B-конкурент) і [CleaningPartner](https://cleaningpartner.com.ua/uk/). Висновок: наш чистий типографічний підхід без стокових фото (рукавички/розпилювачі) вже виглядає дорожче за конкурентів — це зберегти. Запропоновано 4 покращення, користувач погодив 2 з 4:
- ⏸️ Винесений блок-цифри-довіри великим шрифтом — **відкладено**, повернутись пізніше, коли буде більше інформації про компанію
- ⏸️ Чесне формулювання про поточного клієнта (фонд/готелі/промислові об'єкти без назв) — **відкладено**, користувач хоче спершу побачити, як це виглядатиме на сайті
- ✅ Години роботи в контактах — **зроблено**
- ✅ Sticky CTA-кнопка дзвінка на мобільному — **зроблено**

**Що зроблено технічно:**
- Отримано реальні робочі години від користувача: щодня 9:00–19:00. Додано рядком у секцію контактів (з іконкою годинника) і в JSON-LD `openingHoursSpecification` для SEO.
- Додано sticky-панель з кнопкою "Зателефонувати", що з'являється на екранах ≤560px після того, як hero-кнопки скролом зникають з поля зору (IntersectionObserver), ховається знову при поверненні нагору.

**Наступний крок:** Фаза 2 — Google Sheets webhook для збору заявок.

## 2026-08-09 — Прибрано послугу, Фаза 2 (Google Sheets) завершена

**Прибирання після ремонту** видалено зі списку послуг і з JSON-LD `serviceType` — за словами користувача, це послуга для майбутнього окремого напрямку "Faino Clean Home" (приватні квартири), не для поточного B2B-профілю.

**Фаза 2 — Google Sheets:** користувач самостійно (за покроковою інструкцією в чаті) створив таблицю "Faino Clean — Ліди" на акаунті `cleanandclear4449@gmail.com`, вставив Apps Script (`doPost` → `appendRow`) і задеплоїв як Web App ("Виконати як: я", "Хто має доступ: усі"). Отриманий webhook URL:
`https://script.google.com/macros/s/AKfycbx_i2C8fhK9aeZoHFoESG6Ze_sm-Rplt13OTlN6eo5hfSvCMt93GtsLn8YId7VbPwGQ/exec`

Вплетено в `index.html`: при сабміті форми JS одночасно (1) POST'ить на Formsubmit (як і раніше, email) і (2) шле `fetch(SHEETS_WEBHOOK, {mode:'no-cors', body: JSON.stringify({name, phone, type, comment})})` — fire-and-forget, не блокує і не залежить від Formsubmit. Колонки в таблиці: Дата, Час, Ім'я, Телефон, Тип об'єкта, Коментар (дата/час проставляє сам Apps Script на боці Google, timezone Europe/Kyiv).

**Перевірено end-to-end:** тестова заявка через живий сайт (daniil4449.github.io/faino-clean) успішно долетіла в таблицю з коректними даними. Тестовий рядок користувач видаляє сам.

**Наступний крок:** Фаза 3 — Telegram-бот через Make.com (окремий бот/номер від Importica).

## 2026-08-10 — Виправлено: Formsubmit не пересилав листи

**Проблема:** користувач повідомив, що заявки з форми нікуди не приходять (ні на пошту, ні деінде). Діагностовано: форма технічно відправлялась коректно (підтверджено через мережевий запит — iframe навігувався на formsubmit.co), Google Sheets отримував заявки нормально, але Formsubmit вимагає одноразової активації email — перший сабміт на нову адресу лише шле лист-підтвердження, а сама заявка не пересилається, доки не натиснути "Activate Form" у цьому листі.

**Виправлено:**
- Користувач знайшов лист "Action Required: Activate FormSubmit" у Gmail, натиснув Activate Form.
- Форму переведено з "голої" email-адреси (`formsubmit.co/cleanandclear4449@gmail.com`) на рекомендований hash-endpoint (`formsubmit.co/e457095c35f1def47c16dc7fafbcc492`) — приховує адресу від сканерів у вихідному коді сторінки.
- Перевірено end-to-end: тестова заявка успішно прийшла на пошту з коректним форматуванням (таблиця: Ім'я/Телефон/Тип_об'єкта/Коментар).

**Увага:** в поштовій скриньці виявлено лист із заявкою (Виробниче приміщення, 0502313652, "1000 кв м"), що не є тестовою (не позначена як "ТЕСТ") — можливо, реальний лід від відвідувача сайту за час, поки форма не пересилала листи. Користувачу передано перевірити вручну.

**Підтверджено користувачем:** це справді реальний лід — Дамір, виробниче приміщення, 1000 кв м. Заявка потрапила в Google Sheets **двічі** (однаковий час 20:14) — типовий наслідок подвійного тапу/кліку.

## 2026-08-10 — Захист форми від подвійного сабміту

За детальним технічним запитом користувача (з готовим прикладом коду) додано захист від дублікатів заявок:
- Кнопка "Надіслати заявку" блокується **синхронно**, в першому рядку обробника `submit`, до будь-якої асинхронної роботи (`btn.dataset.submitting`, `btn.disabled = true`)
- Текст кнопки міняється на "Надсилаємо..." — видимий фідбек користувачу (важливо на повільному мобільному, де саме "не бачу що спрацювало" провокує повторний тап)
- Через 2с кнопка розблоковується назад — щоб відвідувач міг надіслати другу окрему заявку без перезавантаження сторінки

## 2026-08-13 — Фаза 3 (Telegram-бот): бот, група і базовий вебхук готові

Користувач попросив повний стек як в Importica ("окремий бот, окрема група з темами, як у Importica"). Прогрес за сесію (все зроблено покроково, з живими скріншотами через BotFather/Telegram Web/Make.com):

- **Бот створено** через BotFather: `@fainoclean_kyiv_bot`. Токен зберігається лише в Make.com (у з'єднанні "Faino Clean Bot") — **у публічний репозиторій не потрапляє**, за тим самим принципом, що і в Importica.
- **Група "Faino Clean - Заявки"** створена, увімкнено Topics (форум-режим), бот доданий і призначений адміністратором з повними правами (крім Remain Anonymous).
- **Group Chat ID: `-1003921758523`** (is_forum: true, type: supergroup) — отримано через тестове повідомлення `/test` і "Run once" в Make.com.
- **Make.com:** новий окремий сценарій **"Faino Clean — Telegram Bot"** (навмисно перейменований одразу, щоб не плутати з існуючим сценарієм Importica "Integration Telegram Bot"). Перший модуль — Telegram Bot → Watch Updates, підключений до з'єднання "Faino Clean Bot" (НЕ до "Importica Bot", який Make пропонував за замовчуванням — важливо було це відловити). Перевірено: приходять реальні дані з чату.

**Ще не зроблено (наступні кроки Фази 3):**
- Маршрут: нова заявка з сайту (форма) → створення нової теми в групі → повідомлення в цю тему з даними ліда
- Маршрут "відповідь з теми клієнту" (як у Importica — Danylo пише в темі, бот пересилає клієнту в приват) — потребує зберігати Topic ID в Google Sheets, як у Importica
- Команди бота /start, /help через BotFather
- Фільтри за Chat ID на кожному шляху роутера (як в Importica, щоб уникнути фантомних заявок і самовимкнень сценарію)

**Наступний крок:** продовжити збірку маршрутів у Make.com — почати з "нова заявка → нова тема в групі".
- Перевірено потрійним швидким кліком (симуляція через JS): до фіксу — N окремих запитів; після — рівно 1 запит, кнопка коректно заблокована й розблоковується за розкладом. Перевірено і локально, і на живому задеплоєному сайті.

## 2026-08-21 — Секція "Співпраця"

Додано новий розділ для B2B-партнерів (субпідряд, франчайзинг, інтеграції) між "Про компанію" і "Контакти":
- Пункт "Співпраця" у навігації (`#partnership`), той самий стиль, що й інші пункти меню
- Секція `<section id="partnership">` у тому ж дизайні (section-eyebrow/h2/p, без нових кольорів чи шрифтів) з кнопкою `btn btn-primary` — mailto на cleanandclear4449@gmail.com з темою "Співпраця"
- `sitemap.xml` перевірено — містить лише кореневий URL без якорів, оновлення не потрібне

## 2026-08-21 — Сесія 6: Cloudflare DNS, уточнення Telegram, відкритий баг форми

DNS домену fainoclean.com.ua переведено на Cloudflare (Free план, Imena.ua лишається реєстратором) — старі A/AAAA записи від Imena.ua видалено, додано 4× A на GitHub Pages та CNAME www, MX-запис пошти залишено. Очікується видача SSL-сертифіката від GitHub Pages, фінальна перевірка ще попереду. З'ясувалась плутанина з Telegram-ботом: `@fainoclean_kyiv_bot` і група вже існували з попередньої сесії, спроба створити бота заново вчасно виправлена; розпочато (але не завершено) створення нового сценарію Make.com "Faino Clean — Нова заявка → Telegram" для маршруту заявка→тема. **Відкрито баг:** форма сайту перестала доставляти заявки під час сесії (ні email, ні Sheets) — діагностику перервано, причина невідома, потребує повторної перевірки end-to-end наступного разу.

## 2026-08-21 — Сесія 7: домен готовий, баг форми виправлено

**Фаза 4 завершена:** fainoclean.com.ua повністю робочий, SSL-сертифікат від GitHub Pages видано, сайт відкривається без попереджень браузера. **Баг форми закрито:** причина була саме та, що передбачалась — активація Formsubmit прив'язана до конкретного домену, тож перехід з daniil4449.github.io на fainoclean.com.ua вимагав повторної активації. Переактивовано, перевірено end-to-end одночасно на обох каналах (email + Google Sheets) — обидва підтверджені робочими. Telegram-маршрутизація (Фаза 3) лишається на паузі, зупинено на кроці створення нового сценарію "Faino Clean — Нова заявка → Telegram" в Make.com (натиснуто "+ Create scenario", далі не пішли).

## 2026-08-22 — Сесія 8: Фаза 3 завершена — Telegram-маршрутизація повністю робоча

Довели до кінця маршрут "нова заявка з сайту → нова тема в Telegram-групі":

- Сценарій **"Faino Clean — Нова заявка → Telegram"** в Make.com, 3 модулі: `Webhooks (Custom webhook)` → `Telegram Bot (Make an API Call → createForumTopic)` → `Telegram Bot (Send a Text Message or a Reply)`, в тему через `message_thread_id` з відповіді попереднього кроку.
- Webhook URL: `https://hook.eu1.make.com/o1kr4v95snvbdhg7okdt6pgq6km5jnx1` — вбудований у `TELEGRAM_WEBHOOK` в `index.html`.
- Тригер переведено на **"Immediately as data arrives"** (миттєвий, той самий підхід, що й у Importica) — сценарій активний, не потребує ручного запуску.

**Знайдено і виправлено критичний баг:** перша спроба (JSON.stringify тіла) провалилась з реального сайту — `[400] Bad Request: name must be non-empty`. Причина: `fetch()` з `mode: 'no-cors'` не може реально виставити `Content-Type: application/json` (браузер мовчки ігнорує цей заголовок для no-cors-запитів, лишає `text/plain`), тому Make не міг розпарсити JSON-тіло на іменовані поля — хоча ручні `curl`-тести з явним заголовком проходили без проблем, і саме тому баг не проявився одразу. Виправлено переходом на `new URLSearchParams(leadData)` як тіло запиту — це єдиний формат (`application/x-www-form-urlencoded`), який браузер дозволяє коректно позначити навіть у no-cors-режимі. Google Sheets webhook цю проблему не мав, бо Apps Script сам парсить сирий текст через `JSON.parse()` незалежно від заголовка.

**Перевірено end-to-end з реального домену fainoclean.com.ua:** тестова заявка "ПОВНА ПЕРЕВІРКА ФІНАЛ" одночасно прийшла на email (Formsubmit), у Google Таблицю і створила нову тему в Telegram-групі з коректними даними (включно з кирилицею).

**Фазу 3 закрито.** Лишається на майбутнє (не блокує поточну роботу): маршрут "відповідь у темі → приват клієнту" (потребує зберігати `message_thread_id` в Google Sheets, як в Importica), команди /start /help, фільтри за Chat ID.

## 2026-08-22 — Прибрано "(скоро)" з кнопки Telegram, перевірено телефон

Тепер, коли Telegram-маршрутизація реально працює, текст кнопки в шапці й контактах змінено з "Telegram (скоро)" на просто "Telegram". Заодно перевірено консистентність номера телефону по всьому сайту (7 місць: шапка, hero, контакти ×2, sticky CTA, footer, JSON-LD) — усюди `+380951915964`, розбіжностей не знайдено.

## 2026-08-24 — Google Analytics 4 підключено

Користувач самостійно створив GA4-властивість "Faino Clean" і потік даних для fainoclean.com.ua (Stream ID `15491639623`), отримав Measurement ID `G-S3F8CCMZS7`. У `index.html` плейсхолдер-коментар `<!-- Google Analytics GA4: підключити після створення GA4-властивості -->` замінено на реальний gtag.js snippet (async-завантаження + `gtag('config', 'G-S3F8CCMZS7')`), вставлено в `<head>` до інших тегів. Закомічено й запушено (`4b584b8`). Верифікація через "Обзор в реальном времени" в GA4 — на користувачі.

**Наступний крок:** Google Search Console (домен готовий, лишилось додати/верифікувати властивість і відправити sitemap.xml).

## 2026-08-24 — Google Search Console верифіковано, sitemap надіслано

Властивість "Домен" `fainoclean.com.ua` верифікована через DNS TXT-запис (автоматично додано інтеграцією Google↔Cloudflare). `sitemap.xml` надіслано, статус одразу після відправки "Not received" — очікувано, обробка займає час.

## 2026-08-24 — Email на домені: Cloudflare Email Routing підключено

Налаштовано прийом пошти на `info@fainoclean.com.ua` → пересилання на `cleanandclear4449@gmail.com` через Cloudflare Email Routing (безкоштовно). Старий конфліктний MX-запис `mail.imena.com.ua` видалено вручну через DNS → Records — без цього кроку Cloudflare відмовлявся автододавати власні MX-записи ("Existing non-Cloudflare MX records conflict"). Тестовий лист на info@ дійшов (спершу в Спам — очікувано для домену без історії надсилань, позначено "Це не спам").

**Обмеження безкоштовного Email Routing:** тільки прийом, не дозволяє відповідати ВІД імені info@fainoclean.com.ua — при натисканні "Ответить" Gmail фізично шле від cleanandclear4449@gmail.com. Повне рішення (Google Workspace, ~$7/міс) відкладено на майбутнє.

## 2026-08-25 — Сесія 9/10: підтверджено брендоване ім'я відправника, GA4/GSC/GSC-статус синхронізовано в CLAUDE.md і context.md

Косметичний фікс з попередньої сесії (Gmail → Налаштування → Облікові записи й імпорт → "Надсилати листи від імені" → "Faino Clean" замість "Daniil Denisyuk") підтверджено реальним тестом: отримувач бачить у заголовку листа "від: Faino Clean <cleanandclear4449@gmail.com>". Sitemap у Search Console дійшов до статусу "Success" (1 сторінка проіндексована) — GSC повністю закрито. `CLAUDE.md` і `context.md` синхронізовано з актуальним станом (GA4, GSC, email на домені, попередження про два Cloudflare-акаунти, правило "усі зміни коду — тільки через Claude Code").

**Реально відкриті пункти зараз:**
- Google Workspace для info@fainoclean.com.ua (~$7/міс) — щоб відповіді теж йшли від info@
- Telegram: маршрут "відповідь у темі → приват клієнту"
- Telegram: команди /start і /help
- Відкладені hero-блоки (цифри-довіри, згадка поточних клієнтів)

## 2026-08-25 — Сесія 11: аудит сайту + секція "Вакансії"

**Аудит на запит користувача** (орфографія, русизми, логіка, структура, SEO, готовність до реклами):
- Знайдено і виправлено русизм-помилку: "периодично" → "**періодично**" (опис послуги "Генеральне прибирання").
- JSON-LD `serviceType` доповнено до 5 пунктів (бракувало "Клінінг-модель під об'єкт").
- Виправлено коряву фразу в блоці "Про компанію" ("офіційний партнер... для компаній, що обслуговує" → "офіційний партнер, що обслуговує").
- Синхронізовано текст під кнопкою форми з реальним текстом кнопки ("Надіслати" → "Надіслати заявку").
- Додано `<noscript>`-запобіжник: без нього весь контент сторінки (scroll-reveal, `opacity:0` за замовчуванням) лишався б назавжди невидимим при вимкненому JS.
- Додано трекінг конверсій у GA4, якого раніше не було: подія `generate_lead` при успішній відправці форми (контактної і вакансій) та `phone_click` на всіх `tel:`-посиланнях сайту.
- **Залишено на майбутнє (не код, потребує окремого рішення користувача):** `og-image.svg`/twitter-image варто замінити на PNG — Facebook/Instagram/LinkedIn не рендерять SVG в прев'ю посилань і рекламі; Meta Pixel не підключено (потрібен Pixel ID з Facebook Business Manager); сторінки Політики конфіденційності немає, а вона зазвичай потрібна для Meta/Google Ads, коли сайт збирає особисті дані через форму.

**Нова секція "Вакансії"** (`#vacancies`, останньою перед footer, пункт у навігації):
- 3 відкриті вакансії — Прибиральниця, Мийниця, Працівник кухні — картки у стилі списку послуг, клікабельні (кожна — `<button>`).
- Клік відкриває модальне вікно (не окрема сторінка) з компактною формою: Ім'я, Телефон, Коментар — прихований `Посада` виставляється автоматично залежно від того, яку вакансію натиснули. Закриття: кнопка ×, клік поза модалкою, Escape.
- Заявки йдуть через **той самий** зв'язок, що й основна форма (Formsubmit hash-endpoint, Google Sheets webhook, Telegram webhook Make.com) — свідомо не заводили окремий Sheet/сценарій Make.com, щоб нічого не треба було донастроювати вручну поза кодом. Позиція передається як `type: "Вакансія: <назва>"`, тож заявки кандидатів легко відрізнити від заявок клієнтів у тій самій таблиці/темі Telegram.
- Додано JSON-LD `JobPosting` для кожної з 3 вакансій (title, description, datePosted, hiringOrganization, jobLocation Київ, employmentType: FULL_TIME — уточнити в наступній сесії, якщо треба інший тип зайнятості) — для потрапляння в Google для пошуку роботи.
- Перевірено через headless-браузер: модалка відкривається/закривається коректно (клік, Escape), позиція і фокус виставляються правильно, немає горизонтального overflow на 375px і 1280px.
- **Не протестовано наскрізно (свідомо):** реальну відправку через Formsubmit/Sheets/Telegram не запускали, щоб не створити фейковий лід у робочих каналах. Рекомендація користувачу — зробити один тестовий сабміт із живого сайту після деплою, позначивши ім'я як "ТЕСТ", як і раніше з основною формою.

**Наступний крок:** деплой (`git push`), потім один ручний тест форми вакансій на живому сайті; за бажанням — розглянути og-image в PNG і Політику конфіденційності для готовності до реклами.

## 2026-08-25 — Сесія 11 (продовження): наскрізний тест пройдено, знайдено й виправлено баг Google Sheets

**Деплой і перший тест вакансій:** запушено в `main`, GitHub Pages задеплоїв автоматично. Користувач самостійно надіслав тестову заявку через форму вакансій (посада "Прибиральниця") — email (Formsubmit) і Telegram-тема прийшли коректно з міткою "Тип об'єкта: Вакансія: Прибиральниця", яка чітко відрізняє кандидата від клієнтського ліда.

**Знайдено реальний баг, не пов'язаний з кодом сайту:** у Google Таблиці "Faino Clean — Ліди" тестовий рядок показав у колонці "Коментар" **`#ERROR!`**. Причина: тестовий текст коментаря починався зі знаку `+`, а Google Таблиці за замовчуванням намагаються обчислити будь-яке значення, що починається з `+`, `-`, `=` чи `@`, як формулу. Це стосувалось не тільки нової форми вакансій, а й основної контактної форми — обидві пишуть у той самий Apps Script.

**Виправлено в Apps Script** (`script.google.com`, проєкт "Faino Clean webhook", `Код.gs`, функція `doPost`): замість `sheet.appendRow([...])` тепер `sheet.getRange(...).setNumberFormat('@')` (примусовий текстовий формат) **перед** `range.setValues([row])` — це офіційний спосіб Google унеможливити авто-розпізнавання формул. Користувач самостійно вніс правку через веб-редактор Apps Script і розгорнув як **Версію 2** того самого Web App (`Розгорнути → Керування розгортаннями → редагувати → Нова версія`) — URL webhook лишився незмінним (`...VbPwGQ`), тому `index.html` редагувати не знадобилось.

**Перевірено end-to-end після фіксу:** нова тестова заявка з коментарем, що починається на `+` ("+46464оопшг+4484684684684684"), записалась у таблицю звичайним текстом, без помилки. Email і Telegram підтвердили той самий результат, що й раніше.

**Оновлено `context.md`:** актуалізовано код `doPost` (Версія 2) у розділі "Google Sheets", додано опис знайденого й виправленого бага, закрито пункт "форма вакансій — наскрізна відправка" у відкритих питаннях.

**Реально відкриті пункти зараз:**
- Google Workspace для info@fainoclean.com.ua (~$7/міс)
- Telegram: маршрут "відповідь у темі → приват клієнту", команди /start і /help
- Відкладені hero-блоки (цифри-довіри, згадка поточних клієнтів)
- Готовність до реклами: og-image.svg → PNG, Meta Pixel, Політика конфіденційності
