# Faino Clean — Project Knowledge

**Оновлено:** 26.08.2026 (сесія 14 — двосторонній Telegram-бот (сценарій Make.com `6935862`) протестовано наживо, знайдено й виправлено 3 баги; `CLAUDE.md` замінено повністю окремою Cowork-сесією — новий поділ обов'язків: Claude Code веде файли репозиторію, Cowork-сесія веде Make.com/Google Sheets/DNS/GA4 і синхронізує сюди, у `context.md`)

---

## Компанія

**Faino Clean** — клінінгова компанія для бізнесу (аутсорсинг клінінгу).
- Сайт: https://fainoclean.com.ua (домен підключено 21.08.2026; https://daniil4449.github.io/faino-clean/ лишається робочим як запасний URL)
- Домен: fainoclean.com.ua, реєстратор Imena.ua, DNS через Cloudflare — повністю підключено
- Телефон: +380951915964
- Email: cleanandclear4449@gmail.com (основний, робочий, тепер відображається як "Faino Clean" у листах) + info@fainoclean.com.ua (пересилається на cleanandclear4449@gmail.com через Cloudflare Email Routing, з 24.08.2026)
- Локація: Київ
- Працює з 2013 року, у Києві — з 2014-го
- Юрформа: ФОП 3 групи, офіційний договір
- Робочі години: щодня, 9:00–19:00
- Поточний клієнт: один постійний контракт (благодійний фонд); додаткові ліди — через OLX

**Об'єкти:** офіси, склади, виробничі приміщення, торгові площі, готелі та апартаменти, житлові комплекси (ЖК).

**Послуги (тільки B2B, зараз 5 пунктів):**
1. Аутсорсинг персоналу з прибирання
2. Клінінг-модель під об'єкт
3. Регулярне прибирання за графіком
4. Генеральне прибирання
5. Комплектація об'єкта персоналом

> "Прибирання після ремонту" навмисно **прибрано** зі списку — зарезервовано під майбутній окремий напрямок **Faino Clean Home** (приватні квартири), не для поточного B2B-профілю. Те саме стосується house-іконки в лого (див. розділ "Лого" нижче).

**Ціноутворення:** індивідуальне, залежить від площі/графіка/типу приміщення — на сайті принципово немає фіксованого прайсу.

---

## Технічна інфраструктура

| Сервіс | Статус | Деталі |
|--------|--------|--------|
| Репозиторій | ✅ | github.com/Daniil4449/faino-clean (публічний) |
| Хостинг | ✅ | GitHub Pages, auto-deploy через GitHub Actions при push в `main` |
| Домен | ✅ | fainoclean.com.ua повністю підключено (21.08.2026) — DNS через Cloudflare (Free план), Imena.ua лишається реєстратором. Nameservers: donovan.ns.cloudflare.com, isabel.ns.cloudflare.com. |
| DNS/SSL | ✅ | Cloudflare: 4× A на fainoclean.com.ua → 185.199.108.153, .109.153, .110.153, .111.153 (GitHub Pages, Proxied), CNAME www → daniil4449.github.io (Proxied). Старий MX-запис mail.imena.com.ua видалено 24.08.2026 (конфліктував з Email Routing). SSL-сертифікат від GitHub Pages видано, сайт відкривається без попереджень браузера. |
| Форма заявки | ✅ | Formsubmit.co (hash-endpoint `e457095c35f1def47c16dc7fafbcc492` → cleanandclear4449@gmail.com), POST у прихований iframe. **Важливий урок (21.08.2026):** активація Formsubmit прив'язана до конкретного домену — при переході з daniil4449.github.io на fainoclean.com.ua знадобилась ПОВТОРНА активація (форма коротко "мовчала", ні email ні Sheets). Переактивовано, перевірено end-to-end на обох каналах одночасно. **Якщо домен ще раз зміниться — очікувати той самий крок.** |
| Google Sheets (Ліди) | ✅ | "Faino Clean — Ліди" на акаунті cleanandclear4449@gmail.com, через Apps Script webhook |
| Telegram-бот (заявки з форми) | ✅ | Фаза 3 завершена — нова заявка автоматично створює тему в групі з даними ліда, перевірено end-to-end (деталі нижче) |
| Telegram-бот (двосторонній чат) | ⏳ | Сценарій Make.com `6935862` (org `1718828`) зібраний і протестований наживо 26.08.2026 — 3 баги знайдено й виправлено (деталі нижче). Toggle OFF, активація — після підтвердження гілки "адмін → клієнт" |
| Google Analytics GA4 | ✅ | Підключено 24.08.2026. Ресурс "Faino Clean" (акаунт GA cleanandclear4449@gmail.com, той самий, де вже був Importica). Measurement ID: `G-S3F8CCMZS7`. Тег gtag.js вставлений у `<head>` index.html (Claude Code), перевірено на живому сайті. |
| Google Search Console | ✅ | Верифіковано 24.08.2026 через тип ресурсу "Домен" (не URL-prefix) — DNS TXT-запис, додано автоматично через інтеграцію Google↔Cloudflare. Sitemap `sitemap.xml` надіслано і **успішно оброблено (статус Success, 1 сторінка проіндексована)** — повністю готово. |
| Email на домені | ✅ | Підключено й перевірено 24.08.2026. Cloudflare Email Routing, акаунт cleanandclear4449@gmail.com. Правило: **info@fainoclean.com.ua → cleanandclear4449@gmail.com** (Active). Тестовий лист дійшов end-to-end (спершу впав у Спам — очікувано для нового домену без історії надсилань — позначили "Це не спам", далі листи йдуть у Вхідні). **Обмеження безкоштовного Email Routing:** він тільки приймає пошту на info@ і пересилає у Gmail — не дозволяє відповідати ВІД імені info@fainoclean.com.ua. Коли натискаєш "Ответить" на вхідний лист, Gmail фізично відправляє від cleanandclear4449@gmail.com (реальна скринька), і клієнт бачить саме цю адресу. **Косметичний фікс (зроблено і ПЕРЕВІРЕНО 24–25.08.2026):** у Gmail → Налаштування → Облікові записи й імпорт → "Надсилати листи від імені" змінено ім'я відправника з "Daniil Denisyuk" на **"Faino Clean"**. Підтверджено реальним тестом: отримувач бачить у заголовку листа "від: Faino Clean <cleanandclear4449@gmail.com>" — адреса лишається gmail, але виглядає брендовано. **Повне рішення (щоб відповіді реально йшли від info@fainoclean.com.ua) — Google Workspace (~$7/міс)**, залишається майбутнім кроком, не зроблено. **Технічна деталь на майбутнє:** старий MX-запис `mail.imena.com.ua` конфліктував з автододаванням DNS Email Routing ("Existing non-Cloudflare MX records conflict") — довелось видалити його вручну через DNS → Records перед тим, як "Add missing records" спрацював. |

---

## Google Sheets — деталі (Фаза 2, завершено 09.08.2026)

- **Таблиця:** "Faino Clean — Ліди", акаунт cleanandclear4449@gmail.com
- **Колонки:** Дата | Час | Ім'я | Телефон | Тип об'єкта | Коментар
- **Webhook URL:** `https://script.google.com/macros/s/AKfycbx_i2C8fhK9aeZoHFoESG6Ze_sm-Rplt13OTlN6eo5hfSvCMt93GtsLn8YId7VbPwGQ/exec`
- **Apps Script (`doPost`), поточна версія (Версія 2, 25.08.2026):**
```javascript
function doPost(e) {
  var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  var data = JSON.parse(e.postData.contents);
  var now = new Date();
  var row = [
    Utilities.formatDate(now, 'Europe/Kyiv', 'dd.MM.yyyy'),
    Utilities.formatDate(now, 'Europe/Kyiv', 'HH:mm'),
    data.name || '', data.phone || '', data.type || '', data.comment || ''
  ];
  var newRowIndex = sheet.getLastRow() + 1;
  var range = sheet.getRange(newRowIndex, 1, 1, row.length);
  range.setNumberFormat('@'); // текстовий формат — щоб +, -, =, @ не сприймались як формула
  range.setValues([row]);
  return ContentService.createTextOutput(JSON.stringify({status:'ok'})).setMimeType(ContentService.MimeType.JSON);
}
```
- **В index.html:** при сабміті форми JS одночасно шле на Formsubmit (email) і `fetch(webhook, {mode:'no-cors', body: JSON.stringify({name, phone, type, comment})})` — незалежно одне від одного, fire-and-forget.
- Перевірено end-to-end тестовою заявкою через живий сайт — рядок коректно з'явився в таблиці.
- **Перший реальний лід (09.08.2026, до фіксу Formsubmit):** Дамір, виробниче приміщення, 1000 кв м, тел. 502313652 — знайдено в таблиці Google Sheets (Sheets працювали навіть коли email ще не пересилав). Прийшов подвійним рядком (подвійний тап/клік) — **виправлено 10.08.2026**, див. нижче.
- **Захист від дублів (10.08.2026):** кнопка "Надіслати заявку" синхронно блокується (`disabled=true`) в першому рядку обробника `submit`, до будь-яких асинхронних викликів, з видимим текстом "Надсилаємо..." і авто-розблокуванням через 2с. Перевірено потрійним швидким кліком — рівно 1 запит замість 3.
- **Знайдено і виправлено 25.08.2026: "формула" замість тексту в колонці Коментар.** Якщо значення (найчастіше коментар) починається зі знаку `+`, `-`, `=` чи `@`, Google Таблиці за замовчуванням намагаються обчислити його як формулу — в клітинці з'являється `#ERROR!` замість введеного тексту. Виправлено на рівні Apps Script: перед записом рядка клітинки примусово форматуються як текст (`range.setNumberFormat('@')`), тому такі значення тепер зберігаються буквально. Розгорнуто як нову версію того самого Web App (URL не змінився, правки в `index.html` не знадобились). Перевірено тестовою заявкою з коментарем "+46464оопшг+4484684684684684" — записався коректно.
- **Правильний ID таблиці (важливо, 26.08.2026):** `1CA881EyuR4i6Ilr6FHRrGER8Lr-o3ZOYNdEESU0qPq8` — велика I + маленька l (`Ilr6`), НЕ дві великі I (`IIr6`). Одруковування в ID спричиняло повторювані "Файл не знайдено" при спробах відкрити таблицю за збереженим посиланням; знайдено пошуком файлу в Google Drive за назвою. Перевіряти цей ID символ за символом при повторному використанні.
- **Структуру таблиці розширено** (двосторонній Telegram-бот, сценарій `6935862`) — додано колонки для джерела/Chat ID/Topic ID, потрібні для маршруту "адмін відповідає в темі → клієнт отримує DM". Це змінено окремою Cowork-сесією напряму в Google Sheets, не через Claude Code.

---

## Telegram-бот (Фаза 3, ЗАВЕРШЕНО 22.08.2026)

- **Бот:** @fainoclean_kyiv_bot (t.me/fainoclean_kyiv_bot). Токен НЕ зберігається в репозиторії — тільки у з'єднанні Make.com "Faino Clean Bot".
- **Група:** "Faino Clean - Заявки", Topics (форум-режим) увімкнено, бот — адміністратор. **Group Chat ID:** `-1003921758523`
- **Робочий маршрут "нова заявка → нова тема в групі":** сценарій Make.com **"Faino Clean — Нова заявка → Telegram"** (окремий від старого тестового сценарію "Faino Clean — Telegram-бот", який лишається порожнім заготовком для майбутнього маршруту "відповідь у темі → приват клієнту"). 3 модулі:
  1. `Webhooks (Custom webhook)` — URL: `https://hook.eu1.make.com/o1kr4v95snvbdhg7okdt6pgq6km5jnx1`, вбудований у сайт як `TELEGRAM_WEBHOOK`
  2. `Telegram Bot (Make an API Call)` — `createForumTopic`, POST, body `{"chat_id":-1003921758523,"name":"{{name}}"}`
  3. `Telegram Bot (Send a Text Message or a Reply)` — Chat ID `-1003921758523`, Message Thread ID = `message_thread_id` з відповіді кроку 2, текст з усіма полями заявки
  - Тригер: **"Immediately as data arrives"** (миттєвий, увімкнено, той самий підхід що в Importica)
- **Критичний баг і фікс (22.08.2026):** перший робочий варіант з `fetch(..., {mode:'no-cors', body: JSON.stringify(...)})` провалювався на реальному сайті — `[400] name must be non-empty`, бо no-cors fetch не може реально виставити `Content-Type: application/json` (браузер тихо ігнорує заголовок, лишає `text/plain`), і Make не розбирав тіло на поля. Виправлено переходом на `body: new URLSearchParams(leadData)` — єдиний формат (`application/x-www-form-urlencoded`), який browser дозволяє коректно позначити в no-cors режимі. **Якщо колись знову міняти формат тіла цього fetch-запиту — пам'ятати про це обмеження.**
- **На сайті:** кнопка тепер просто "Telegram" (без "(скоро)") в шапці й контактах — текст прибрано після підтвердження, що маршрутизація вже реально працює.
- **Ще не зроблено (не блокує, на майбутнє):** маршрут "відповідь у темі → приват клієнту" (потребує зберігати `message_thread_id` в Google Sheets, як в Importica), команди /start /help, фільтри за Chat ID на шляхах роутера.
- **Перевірено end-to-end 22.08.2026** з реального домену fainoclean.com.ua: одна тестова заявка одночасно прийшла на email, у Google Sheets і створила тему в Telegram з коректними даними (включно з кирилицею).

### Двосторонній Telegram-бот (сценарій `6935862`, тестування 26.08.2026)

Окремий сценарій Make.com "Faino Clean — Telegram Bot" (ID `6935862`, org `1718828`), 4 гілки роутера, зібраний у попередній сесії — ця сесія (14) провела перше наскрізне живе тестування. Знайдено й виправлено 3 баги:

1. **`createForumTopic` падав з `[400] chat_id is empty`** — модуль "Make an API Call" мав Body Type = "Map Body" (сирий JSON-текст), ненадійно передавав числовий `chat_id`. Фікс: Body Type = "Assemble Body" (структурований Key/Value UI).
2. **Дата/Час у Google Sheets записувались буквальним сміттям** (`{{"26.08.2026"}}` замість `26.08.2026`) — поле `formatDate(now; ...)` виглядало коректно в редакторі Make (підтверджено zoom-скріншотом), але реальний запуск все одно писав побитий текст. **Урок: вигляд raw-поля з функцією в редакторі Make ніколи не доказ коректності — перевіряти тільки реальний output.** Фікс: поле повністю очищено й набрано заново через панель Date/time.
3. **Дублювання тем через "фантомні" `forum_topic_created`-події** — коли бот сам створює тему через `createForumTopic`, Telegram надсилає в той самий вебхук службову подію без поля `text`. Гілка "Тільки з групи" перевіряла лише `Chat:ID`, намагаючись обробити цю подію як відповідь адміна. Фікс: додано другу умову до фільтра — `Message:Text` **Exists** (AND). **Урок: будь-яка дія бота, що сама генерує апдейт у групі, повертається в той самий вебхук — фільтр "тільки повідомлення з групи" має додатково перевіряти наявність тексту.**

**Підтверджено живими повідомленнями:** `/start`, `/help`, "новий клієнт" (нова тема + приватне вітання + картка заявки + запис у Sheets), "повторний клієнт" (лягає в існуючу тему, не створює нову).

**Не підтверджено:** гілка "адмін відповідає в темі → клієнт отримує приватний DM" — технічно готова й полагоджена (той самий фікс бага №3 її стосується), але жодного живого повідомлення в цьому напрямку ще не надіслано.

**Статус:** сценарій toggle OFF, Sequential processing = Yes, черга вебхука порожня. Активація — після підтвердження гілки "адмін → клієнт". Повний опис і технічні прийоми — `DEVELOPMENT_LOG.md`, сесія 14.

---

## Лого

Фінальні векторні файли отримано від власника бренду, вставлені **без жодних змін геометрії**:
- `logo-green.svg`, `logo-black.svg` — повний лого-блок (напис "Faino Clean" + іконка будівлі)
- `logo-building-green.svg` — тільки іконка будівлі, без напису (для favicon/apple-touch-icon)

**Іконка будівлі** — правильний варіант під поточний B2B-профіль, використовується всюди (шапка, hero, favicon).
**Іконка будиночка** (є в мокапах, файл поки не передано) — зарезервована під майбутній **Faino Clean Home** (приватні квартири), НЕ використовувати на поточному сайті.

**Колір бренду:** пройшов дві ітерації правок за фідбеком користувача (вихідний `#133E25` читався як майже чорний на деяких моніторах) → `#1B6E3C` → фінальний **`#1A5E33`**. Застосовано в усіх лого-файлах і в CSS-змінній `--accent` (кнопки, іконки на сайті), щоб усе було єдиного відтінку.

Футер сайту — принципово **без іконки**, тільки текст "Faino Clean" — пряма вимога користувача.

**og-image (25.08.2026):** `og-image.png` (1200×630, для `og:image`/`twitter:image`) використовує **повністю незмінений** `logo-green.svg` (не перемальовано вручну — вставлено як є) + тег-лайн і домен. Причина: спроба відтворити лого вручну (текст + окрема іконка) виявила, що `logo-building-green.svg` містить дрібний декоративний штрих у вихідному векторі, непомітний при малому розмірі (шапка/favicon), але помітний при збільшенні — щоб не перемальовувати фінальний файл, вирішено просто вставляти готовий `logo-green.svg` цілком, а не відтворювати його елементи окремо.

---

## Дизайн-система (не змінювати без прямого запиту)

```css
--bg: oklch(96.5% 0.012 145)
--bg-alt: oklch(93.5% 0.016 145)
--ink: oklch(24% 0.045 155)      /* колір тексту/заголовків — навмисно майже чорний, НЕ те саме що --accent */
--ink-soft: oklch(38% 0.03 150)
--line: oklch(85% 0.02 150)
--accent: #1A5E33                 /* брендовий зелений — кнопки, іконки, лого */
--dark: oklch(17% 0.025 155)
--paper: oklch(98% 0.006 100)
```
Заголовки — Playfair Display (500–700), текст — Outfit (300–600). Inter заборонено.
Іконки — тонкі лінійні (stroke ~1.4–1.6px), без кольорових підложок. Без нумерації розділів 01/02/03.
Позиціонування B2B: без "з душею", без емодзі-будиночків, без кітчу (мітли, бульбашки) — і принципово без стокових фото (рукавички/розпилювачі), які роблять сайти конкурентів "дешевими".

**Адаптивність:** перевірено реальним виміром (`scrollWidth` vs `innerWidth`, не на око) — без горизонтального overflow на 320px, 375px, 768px, 1024×600 (нетбук), 1920px. Breakpoints: 880px, 560px, 380px.

---

## Структура сайту (секції index.html по порядку)

1. Header (sticky) — лого + nav + кнопка "Зателефонувати"
2. Hero — заголовок, підзаголовок, 2 CTA, trust-row (з 2013 року / ФОП, договір / 6 типів об'єктів), логотип-мітка справа
3. Стрічка типів об'єктів — 6 пунктів з лінійними іконками
4. Послуги — плоский список, 5 пунктів (без "після ремонту")
5. Чому ми — інвертована темна секція, 5 пунктів
6. Про компанію / Історія — 2013 → 2014 Київ, стримано, без драматизації
7. Співпраця — для B2B-партнерів (субпідряд, франчайзинг, інтеграції), кнопка mailto з темою "Співпраця" (додано 21.08.2026, в окремій сесії)
8. Контакти — телефон, email, Київ, години роботи (9:00–19:00), Telegram, форма заявки
9. Вакансії (`#vacancies`, додано 25.08.2026) — 3 відкриті посади (Прибиральниця, Мийниця, Працівник кухні), клік відкриває модальну форму заявки; заявки йдуть через ту саму інфраструктуру, що й основна форма (Formsubmit + Google Sheets + Telegram), з позначкою `Вакансія: <назва>` для відрізнення від клієнтських лідів. JSON-LD `JobPosting` на кожну вакансію.
10. Footer — мінімальний, лого-текст без іконки
+ Sticky mobile CTA — кнопка "Зателефонувати", з'являється при скролі повз hero на екранах ≤560px (IntersectionObserver)
+ GA4-конверсії (додано 25.08.2026): подія `generate_lead` при відправці будь-якої з двох форм, `phone_click` на всіх tel:-посиланнях

---

## Відкриті питання (потребують уваги наступної сесії)

- ~~Баг форми~~ — ✅ вирішено 21.08.2026, див. рядок "Форма заявки" в таблиці інфраструктури вище (причина: Formsubmit-активація прив'язана до домену).
- ~~SSL-сертифікат~~ — ✅ видано, fainoclean.com.ua відкривається без попереджень.
- ~~Google Search Console / GA4~~ — ✅ обидва повністю завершено 24.08.2026 (включно з sitemap: статус Success), див. таблицю інфраструктури вище.
- ~~Telegram-маршрутизація (Фаза 3)~~ — ✅ завершено 22.08.2026, див. розділ "Telegram-бот" вище.
- ~~Email на домені (прийом + ім'я відправника)~~ — ✅ завершено й перевірено 24–25.08.2026, див. таблицю інфраструктури вище.

**Реально відкриті пункти зараз:**
- Google Workspace для info@fainoclean.com.ua (~$7/міс) — щоб ВІДПОВІДІ клієнтам теж йшли від info@, а не від cleanandclear4449@gmail.com. Поки не зроблено, тільки прийом налаштовано.
- Telegram: маршрут "відповідь у темі → приват клієнту" — технічно готовий і полагоджений (сесія 14), але не підтверджений живим повідомленням; сценарій `6935862` лишається toggle OFF до цього тесту
- ~~Telegram: команди /start і /help~~ — ✅ підтверджено живими повідомленнями 26.08.2026
- Відкладені hero-блоки: цифри-довіри, згадка поточних клієнтів (чекають рішення користувача)
- **CLAUDE.md замінено повністю 26.08.2026** (підготовлено в Cowork-сесії) — прибрано окремі блоки "⚠️ Правило роботи: тільки Claude Code" і "⚠️ Два окремі акаунти Cloudflare", які раніше виносились на початок файлу. Обидва застереження й далі є в цьому `context.md` (розділи нижче) — просто перевірити з користувачем, чи не варто повернути їх і в `CLAUDE.md`.
- **Готовність до реклами:** ✅ `og-image.png` (1200×630) готово й підключено 25.08.2026 — див. розділ "Лого" нижче. Ще відкрито: Meta Pixel не підключено (потрібен Pixel ID з Facebook Business Manager, зовнішній кабінет); Політики конфіденційності на сайті немає, хоча форма збирає особисті дані — зазвичай вимагається для Meta/Google Ads
- ~~Форма вакансій — наскрізна відправка~~ — ✅ перевірено 25.08.2026 (email + Google Sheets + Telegram), включно з крайовим випадком (коментар, що починається на "+"), див. розділ "Google Sheets" вище.

---

## Фазований план (користувач хоче послідовно, не все одразу)

1. ✅ Сайт + Formsubmit + базове SEO + GitHub Pages
2. ✅ Google Sheets webhook для збору заявок
3. ✅ Telegram-бот через Make.com — маршрутизація заявок готова й перевірена
4. ✅ Підключення домену fainoclean.com.ua (Cloudflare DNS) — повністю готово
5. ✅ GA4 + Google Search Console — підключено й верифіковано 24.08.2026
6. ✅ Email на домені (Cloudflare Email Routing, прийом на info@ + брендоване ім'я відправника) — підключено й перевірено 24–25.08.2026

**Відкладено користувачем (не забуто, повернутись пізніше):**
- Великий типографічний блок-цифри-довіри в hero (чекає на більше інформації про компанію)
- Чесне формулювання про поточного клієнта (фонд/готелі/промислові об'єкти без назв) — хоче спершу побачити на маківці/сайті
- Google Workspace для повноцінного надсилання від info@fainoclean.com.ua (~$7/міс)

---

## Файли проєкту

```
faino-clean/
├── index.html              ← весь сайт (HTML+CSS+JS в одному файлі)
├── logo-green.svg           ← основне лого (шапка, hero)
├── logo-black.svg           ← запасний варіант, поки не використовується на сайті
├── logo-building-green.svg  ← тільки іконка (favicon)
├── favicon.svg
├── og-image.svg
├── robots.txt / sitemap.xml
├── CNAME                    ← fainoclean.com.ua
├── CLAUDE.md                 ← контекст для Claude Code CLI
├── context.md                 ← цей файл, для Claude.ai Project Knowledge
├── DEVELOPMENT_LOG.md        ← детальна історія по сесіях
├── README.md
└── .github/workflows/deploy.yml
```

## Типові задачі

```bash
git add index.html
git commit -m "опис змін"
git push origin main
# → GitHub Actions автоматично деплоїть на GitHub Pages, ~30-60 сек
```

Перед новою сесією — звірятись з `DEVELOPMENT_LOG.md` (детальна хронологія) і цим файлом (знімок поточного стану).

---

## ⚠️ ВАЖЛИВО: два окремі акаунти Cloudflare

У користувача **два різні** Cloudflare-акаунти під різними email — легко переплутати:
- **daniilka4449@gmail.com** (особиста пошта) → домен **importica.com.ua** (проєкт Importica)
- **cleanandclear4449@gmail.com** (бізнес-пошта Faino Clean) → домен **fainoclean.com.ua**

Той самий поділ і в Google-акаунтах для GA4/Search Console — обидва сервіси Faino Clean підключені на **cleanandclear4449@gmail.com** (акаунт GA/GSC там же містив і Importica — тобто один Google-акаунт, кілька ресурсів усередині). Якщо потрібен доступ до DNS/Cloudflare-налаштувань fainoclean.com.ua — заходити ТІЛЬКИ під cleanandclear4449@gmail.com, інакше домену просто не буде видно в акаунті.

## ⚠️ Правило роботи: усі зміни коду/файлів — тільки через Claude Code

Користувач (сесія 24.08.2026) прямо попросив: будь-яку задачу на зміну коду чи файлів адресувати Claude Code (CLI), не давати як ручні кроки користувачу — щоб уникнути помилок. Ручні кроки лишаються тільки для дій у зовнішніх вебінтерфейсах (Google Analytics, Search Console, Cloudflare dashboard тощо), які не є кодом/файлами репозиторію.
